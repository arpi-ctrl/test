# Repository Notes

This branch intentionally avoids committing local binary assets because the PR tooling rejects binary files.

- Keep image references as hosted URLs in `index.html`.
- Do not commit files under `assets/`.
- Common local image extensions are ignored via `.gitignore`.
